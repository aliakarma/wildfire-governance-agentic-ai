# Minimal runtime deps for the dashboard's live-simulation path.
# Deliberately excludes torch / geopandas / rasterio / mlflow — the live viewer
# uses the greedy policy and never touches the geospatial or PPO-training code,
# so this keeps the container small and avoids GDAL build pain.
numpy>=1.26,<2.0
scipy>=1.12
pandas>=2.2
cryptography>=42.0
pynacl>=1.5
structlog>=24.1
pyyaml>=6.0
