[pytest]
testpaths = tests
# The package lives under src/ and is not necessarily pip-installed in a
# reviewer's checkout, so put it on the path here rather than requiring
# PYTHONPATH=src on every invocation.
pythonpath = src .
timeout = 120
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
